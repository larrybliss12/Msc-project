# Core analysis (sufficient for `audit`, `analyse`, tests and synthetic validation)
numpy>=1.24
pandas>=2.0
statsmodels>=0.14
tabulate>=0.9

# Corpus access
datasets>=2.18
huggingface_hub>=0.23
soundfile>=0.12
librosa>=0.10

# ASR inference (GPU strongly recommended)
torch>=2.1
transformers>=4.40
accelerate>=0.29

# Testing
pytest>=7.4
